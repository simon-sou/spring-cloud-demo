spring:
  cloud:
    nacos:
      config:
        group: simon
        file-extension: yaml
        server-addr: ${NACOS_SERVER_ADDR:localhost:8848}
      discovery:
        group: simon
        server-addr: ${NACOS_SERVER_ADDR:localhost:8848}
      username: nacos
      password: nacos
